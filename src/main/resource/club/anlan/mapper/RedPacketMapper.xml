<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="club.anlan.mapper.RedPacketMapper" >

    <!--查询红包具体信息-->
    <select id="getRedPacket" resultType="club.anlan.pojo.RedPacket">
        select id,user_id,amount,send_time,total,unit_amount,stock,version,note
        from red_packet
        where id = #{id}
    </select>

    <!--扣减红包库存-->
    <update id="decreaseRedPacket">
        update red_packet set stock = stock - 1 where id = #{id}
    </update>

    <!--新增红包-->
    <insert id="addNewRedPacket" useGeneratedKeys="true" keyProperty="id" parameterType="club.anlan.pojo.RedPacket">
        insert into user_red_packet (red_packet_id,user_id,amount,grab_time,note)
        values (#{redPacketId},#{userId},#{amount},now(),#{note})
    </insert>



</mapper>